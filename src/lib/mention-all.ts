/**
 * The word after the @ that addresses the whole group.
 *
 * Kept apart from lib/comments.ts because that file imports the database, and
 * the composer (a client component) needs the same word the server matches on.
 */
export const EVERYONE = "כולם";
